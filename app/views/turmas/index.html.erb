<%- model_class = Turma -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:id) %></th>
      <th><%= model_class.human_attribute_name(:nome) %></th>
      <th><%= model_class.human_attribute_name(:professora) %></th>
      <th><%= model_class.human_attribute_name(:professora_auxiliar) %></th>
      <th><%= model_class.human_attribute_name(:alunos) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @turmas.each do |turma| %>
      <tr>
        <td><%= link_to turma.id, turma_path(turma) %></td>
        <td><%= turma.nome %></td>
        <td><%= turma.professora %></td>
        <td><%= turma.professora_auxiliar %></td>
        <th><%= turma.alunos.count %></th>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_turma_path(turma), :class => 'btn btn-mini' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      turma_path(turma),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-mini btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_turma_path,
            :class => 'btn btn-primary' %>
