<%= form_for @turma, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :nome, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :nome, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :professora, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :professora, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :professora_auxiliar, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :professora_auxiliar, :class => 'text_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                turmas_path, :class => 'btn' %>
  </div>
<% end %>
